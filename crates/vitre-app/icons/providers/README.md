Provider marks reuse the SVG geometry from this repository's
`apps/web/src/components/Icons.tsx` (MIT). JSX-only attributes are removed;
GPUI applies the current foreground color, with Claude's existing terracotta
accent. Keep these in sync with the web provider icon mapping.
