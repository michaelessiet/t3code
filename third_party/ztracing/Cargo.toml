[package]
name = "ztracing"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false
description = "MIT no-op stand-in for zed's GPL ztracing crate, injected via [patch] (zed issue #55470)"

[dependencies]
tracing = "0.1"
ztracing-macro = { path = "../ztracing-macro" }
